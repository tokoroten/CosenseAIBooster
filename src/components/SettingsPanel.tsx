// 元のファイルは単に settings フォルダのファイルを再エクスポートする
import SettingsPanel, { Tabs, PromptsTab, GeneralTab, ApiTab } from './settings';

export { Tabs, PromptsTab, GeneralTab, ApiTab };
export default SettingsPanel;
